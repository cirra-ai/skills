name: Package & Publish

on:
  push:
    branches: [main]
  release:
    types: [created]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  package:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Package plugins and skills
        run: ./scripts/package-all.sh

      - name: Upload plugin artifacts
        uses: actions/upload-artifact@v6
        with:
          name: plugin-zips
          path: install/plugins/*.zip

      - name: Upload skill artifacts
        uses: actions/upload-artifact@v6
        with:
          name: skill-zips
          path: install/skills/*.zip

      # On every push to main: update the 'preview' pre-release.
      # Pre-releases have publicly accessible download URLs, which is required
      # for skills.cirra.ai/preview/ download links to work. Draft release
      # assets are not publicly accessible, so a pre-release is needed here.
      - name: Update preview pre-release
        if: github.event_name == 'push' || github.event_name == 'workflow_dispatch'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          COMMIT_MSG: ${{ github.event.head_commit.message }}
        run: |
          git tag -f preview
          git push origin preview --force
          gh release delete preview --yes 2>/dev/null || true
          gh release create preview \
            --title "Preview build" \
            --notes "Latest build from \`main\` — ${COMMIT_MSG}" \
            --prerelease \
            install/plugins/*.zip install/skills/*.zip

      # On every push to main: create/replace a draft release ready to be
      # renamed and published as the official release.
      - name: Create draft release
        if: github.event_name == 'push' || github.event_name == 'workflow_dispatch'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          COMMIT_MSG: ${{ github.event.head_commit.message }}
        run: |
          git tag -f next
          git push origin next --force
          gh release delete next --yes 2>/dev/null || true
          gh release create next \
            --title "Draft release" \
            --notes "Packaged from \`main\` — ${COMMIT_MSG}" \
            --draft \
            install/plugins/*.zip install/skills/*.zip

      # On explicit release creation: attach zips to the named release.
      - name: Attach to release
        if: github.event_name == 'release'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          RELEASE_TAG: ${{ github.event.release.tag_name }}
        run: |
          for zip in install/plugins/*.zip install/skills/*.zip; do
            gh release upload "${RELEASE_TAG}" "$zip" --clobber
          done
